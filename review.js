// in python array referred as a list
//  in pyhton you can check whether element is in list or not bt print("a" in list)


// in order to define the index of the element inside of the list we should use method:
// list.index("element")

// -------------ADD---------
// in order to add something new to our list we should use specific methods like append, insert and others.
// ther eare three ways of adding new element to list
// 1) a.append("a")
// 2) a.insert(index, newelement)
// 3) a += ["newelement"]
// 4) a.extend(["a", "b"])

// -------------DELETE---------
//  to delete the list entirely from file you use (del list)
// to empty your list you should use list.clear() method
// to delete the last element in the list we use pop()
//  list.pop()

// --------------CHANGE---------------